// @flow

/**
 * Type declaration for MediaFileType.
 */
type MediaFileType = {
    mimeType: string,
    recorded: string,
    orientation: number,
    width: number,
    height: number,
    fileSize: number,
    duration: number
};
